# WhereCanWeGo

## Development Setup

First, clone this GitHub repository.
<br>
After cloning, run

```
npm install
```

in the root of the cloned directory.

After `npm install` completes, run

```
npm run dev
```

in the same directory to start the development server and begin developing!
